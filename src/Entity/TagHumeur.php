<?php

namespace App\Entity;

use App\Repository\TagHumeurRepository;
use Doctrine\Common\Collections\ArrayCollection;
use Doctrine\Common\Collections\Collection;
use Doctrine\ORM\Mapping as ORM;

#[ORM\Entity(repositoryClass: TagHumeurRepository::class)]
class TagHumeur
{
    #[ORM\Id]
    #[ORM\GeneratedValue]
    #[ORM\Column]
    private ?int $id = null;

    #[ORM\Column(length: 255)]
    private ?string $name = null;

/*     #[ORM\Column(length: 255)]
    private ?string $category = null; */

/*     #[ORM\ManyToMany(targetEntity: Vetement::class, mappedBy: 'tag')]
    private Collection $vetements; */

    public function __construct()
    {
        $this->vetements = new ArrayCollection();
    }

    public function getId(): ?int
    {
        return $this->id;
    }

    public function getName(): ?string
    {
        return $this->name;
    }

    public function setName(string $name): self
    {
        $this->name = $name;

        return $this;
    }

/*     public function getCategory(): ?string
    {
        return $this->category;
    }

    public function setCategory(string $category): self
    {
        $this->category = $category;

        return $this;
    } */

    /**
     * @return Collection<int, Vetement>
     */
    /* public function getVetements(): Collection
    {
        return $this->vetements;
    }

    public function addVetement(Vetement $vetement): self
    {
        if (!$this->vetements->contains($vetement)) {
            $this->vetements->add($vetement);
            $vetement->addTagHumeur($this);
        }

        return $this;
    }

    public function removeVetement(Vetement $vetement): self
    {
        if ($this->vetements->removeElement($vetement)) {
            $vetement->removeTagHumeur($this);
        }

        return $this;
    } */

    public function __toString(){
        return $this->name; //or anything else
    }
}
